# Online Planning in Partially Observable Mazes
Install dependency: 
```
git clone https://github.com/smorad/popgym
cd popgym
pip install .
```
To run the script for both MCTS and forward search: `python main.py`.
